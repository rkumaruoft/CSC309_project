{
  "name": "cssu-rewards-root",
  "private": true,
  "version": "1.0.0",
  "description": "Root runner for CSSU Rewards backend + frontend",
  "scripts": {
    "start": "concurrently \"npm --prefix backend start\" \"npm --prefix frontend run dev\"",
    "install-all": "npm --prefix backend install && npm --prefix frontend install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "all": "^0.0.0"
  }
}
